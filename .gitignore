# Real trade data — never commit (contains margins, partner terms, prices)
trades/*.json
# Sanitized examples (dummy data) ARE tracked — needed by the verify harness
!trades/sample-*.json

# Secrets & local config
.env
.env.local
*.local

# Tax law source documents (large / licensed)
*.pdf

# Node
node_modules/

# OS files
.DS_Store
out/
out/

# Claude Code internal (worktrees, local config)
.claude/
